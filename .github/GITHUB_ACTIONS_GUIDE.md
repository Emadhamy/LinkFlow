# 🚀 GitHub Actions للبناء التلقائي

دليل استخدام GitHub Actions لبناء LinkFlow APK تلقائياً

## 📋 المحتويات

- [الإعدادات الأولية](#-الإعدادات-الأولية)
- [الـ Workflows المتاحة](#-الـ-workflows-المتاحة)
- [الاستخدام](#-الاستخدام)
- [حل المشاكل](#-حل-المشاكل)

---

## ⚙️ الإعدادات الأولية

### 1️⃣ تفعيل GitHub Actions

```bash
# تأكد من أن المشروع مرفوع على GitHub
git add .
git commit -m "⚙️ GitHub Actions setup"
git push origin main
```

### 2️⃣ تفعيل Actions في الـ Repository

1. اذهب إلى: **GitHub Repository → Actions**
2. ستجد workflows مفعلة تلقائياً
3. اضغط على أي workflow لرؤية التفاصيل

---

## 📊 الـ Workflows المتاحة

### Workflow 1: Build APK (`build-apk.yml`)

**التشغيل التلقائي:** عند كل `push` أو `pull request`

**الخطوات:**
1. ✅ تثبيت Java 17
2. ✅ تثبيت Android SDK
3. ✅ تثبيت Cordova
4. ✅ بناء APK (Debug و Release)
5. ✅ رفع الملفات كـ Artifacts

**الملفات المُنتجة:**
- `LinkFlow-Debug` (APK Debug للاختبار)
- `LinkFlow-Release` (APK Release للنشر)

---

### Workflow 2: Build & Sign (`build-release.yml`)

**التشغيل اليدوي:** عند الضغط على `workflow_dispatch` أو عند إنشاء `tag`

**الخصائص:**
- 🎯 بناء موقّع (Signed)
- 📦 إنشاء Release تلقائياً على GitHub
- 🏷️ يُشغّل تلقائياً عند الـ Tags (v1.0.0, v2.0.0, إلخ)

---

## 🎮 الاستخدام

### الطريقة 1: البناء التلقائي (الافتراضي)

```bash
# فقط اعمل push على الـ main branch
git add .
git commit -m "Update app"
git push origin main

# ✅ GitHub Actions ستبني APK تلقائياً
```

### الطريقة 2: البناء اليدوي

1. اذهب إلى: **GitHub → Actions**
2. اختر: **Build & Sign APK**
3. اضغط: **Run workflow**
4. اختر `build_type`: `debug` أو `release`
5. اضغط: **Run workflow** ✅

### الطريقة 3: إنشاء Release (Release Version)

```bash
# أنشئ tag جديد
git tag v1.0.0
git push origin v1.0.0

# ✅ GitHub Actions ستبني وتنشئ Release تلقائياً
```

---

## 📥 تحميل النتيجة

### بعد اكتمال البناء:

1. اذهب إلى: **GitHub → Actions**
2. اختر آخر workflow
3. في قسم **Artifacts**، حمّل:
   - `LinkFlow-Debug` (للاختبار)
   - `LinkFlow-Release` (للنشر)

### أو من Release Page:

1. اذهب إلى: **GitHub → Releases**
2. حمّل APK من آخر Release

---

## 🔧 حل المشاكل

### ❌ Build Failed: "ANDROID_HOME not found"

**الحل:**
الـ workflow يثبتها تلقائياً، لكن إذا حدثت مشكلة:

```bash
# تحقق من الـ logs في Actions tab
# عادة تُحل تلقائياً في المحاولة التالية
```

### ❌ APK غير موجود في Artifacts

**السبب:** قد تكون هناك مشكلة في البناء

**الحل:**
1. افتح الـ workflow run
2. اضغط على أيقونة التفاصيل
3. اقرأ الـ logs
4. ابحث عن الخطأ

### ❌ Workflow لا يعمل

**التحقق:**
```bash
# تأكد من أن الملفات موجودة
ls -la .github/workflows/

# يجب أن تجد:
# build-apk.yml
# build-release.yml
```

---

## 📊 مثال: Workflow Run الكامل

```
✅ Start
  ↓
📥 Checkout code
  ↓
🔧 Setup Java 17
  ↓
📦 Setup Node.js
  ↓
🤖 Setup Android SDK
  ↓
📱 Install Cordova
  ↓
📝 Create Cordova project
  ↓
📁 Copy files
  ↓
🔨 Build APK (Debug)
  ↓
🎯 Build APK (Release)
  ↓
📍 Find APK files
  ↓
⬆️ Upload Debug APK
  ↓
⬆️ Upload Release APK
  ↓
📊 Build Summary
  ↓
✅ Success
```

**الوقت الإجمالي:** 15-20 دقيقة

---

## 💡 النصائح

### 1. تسريع البناء

```bash
# استخدم cache لـ npm
# الـ workflow يستخدمها تلقائياً
```

### 2. إضافة branch حماية

```
Repository → Settings → Branches
  → Add branch protection rule
    → Require status checks to pass
```

### 3. إرسال إشعارات

```bash
# أضف Slack/Discord notification
# لتنبيهات عند اكتمال البناء
```

---

## 📝 الملفات الضرورية

تأكد من وجود هذه الملفات:

```
.github/
├── workflows/
│   ├── build-apk.yml        ← البناء التلقائي
│   └── build-release.yml    ← النشر المتقدم
└── copilot-instructions.md
```

---

## 🚀 الخطوات التالية

### بعد الإعداد الأول:

1. ✅ اختبر الـ workflow يدويًا
2. ✅ تأكد من تحميل APK
3. ✅ اختبر APK على جهاز
4. ✅ هيّئ للنشر على Google Play

---

## 📞 الدعم

### إذا واجهت مشاكل:

1. **اقرأ الـ logs:**
   - GitHub → Actions → Run → Logs

2. **تحقق من الملفات:**
   - `.github/workflows/*.yml`
   - `config.xml`
   - `package.json`

3. **ابحث عن الخطأ:**
   - GitHub Actions Documentation
   - Cordova Troubleshooting Guide

---

## ✨ الخلاصة

**الآن لديك:**
- ✅ بناء تلقائي عند كل push
- ✅ بناء يدوي عند الطلب
- ✅ إنشاء Release تلقائي للـ Tags
- ✅ تحميل وتخزين APK آلي

**اپشتروع الآن جاهز للإنتاج! 🚀**

---

**آخر تحديث:** 2026-01-24
